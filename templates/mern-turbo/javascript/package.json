{
  "name": "mern-turbo-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "MERN monorepo (client + server) powered by Turborepo",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "prettier": "^3.3.3"
  }
}
